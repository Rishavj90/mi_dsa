#include <bits/stdc++.h>
using namespace std;

bool is_blocked(int x, int y, vector<vector<int>>& blocked){
    int lim = 10e6;
    vector<pair<int,int>>vec = {{1,0},{-1,0},{0,-1},{0,1}}; //down, up, left, right
    if(x < 0 || y < 0 || x >= lim || y >= lim)return false;
    for(a)
}

bool isEscapePossible(vector<vector<int>>& blocked, vector<int>& source, vector<int>& target) {
    for()
}